server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    location / { try_files $uri $uri/ /index.html; }
    location = /api/smoke-test {
        default_type application/json;
        return 200 '{"status":"ok","checks":[{"name":"frontend","status":"ok"}]}';
    }
}
